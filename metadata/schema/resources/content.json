{
  "title": "Content",
  "description": "Describes content for a submission.",
  "additionalProperties": false,
  "type": "object",
  "required": ["uncompressed_size", "checksum", "format", "identifier"],
  "properties": {
    "location": {
      "description": "Location of the content resource.",
      "type": "string",
      "format": "url"
    },
    "checksum": {
      "description": "MD5 checksum of the content resource.",
      "type": "string"
    },
    "format": {
      "description": "Content format; e.g. tex, pdf, pdftex, html",
      "type": "string"
    },
    "mime_type": {
      "description": "MIME-type of the content resource.",
      "type": "string"
    },
    "identifier": {
      "description": "Unique identifier for the content resource.",
      "type": "integer"
    },
    "uncompressed_size": {
      "description": "Content size of the uncompressed submission, in bytes.",
      "type": "integer"
    },
    "compressed_size": {
      "description": "Content size of the compressed submission, in bytes.",
      "type": "integer"
    }
  }
}
