{
  "title": "Client",
  "description": "Data about an API client",
  "type": "object",
  "additionalProperties": false,
  "required": ["client_id", "email"],
  "properties": {
    "client_id": {
      "description": "Native ID of the client.",
      "type": "string"
    },
    "client_name": {
      "description": "Name of the client",
      "type": "string"
    },
    "email": {
      "description": "Contact email address for the client",
      "type": "string",
      "format": "email"
    }
  }
}
