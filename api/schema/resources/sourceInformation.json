{
  "title": "SubmissionSourceInformation",
  "description": "Describes a source package for a submission.",
  "required": ["submission_id", "source"],
  "additionalProperties": false,
  "type": "object",
  "properties": {
    "submission_id": {
        "description": "arXiv submission identifier.",
        "readOnly": true,
        "type": "string"
    },
    "source": {
      "oneOf": [
        {
          "type": "object",
          "required": ["href", "checksum", "format", "upload_id"],
          "parameters": {
            "href": {
              "description": "Location of the source package resource.",
              "type": "string",
              "format": "url"
            },
            "checksum": {
              "description": "MD5 checksum of the source package resource.",
              "type": "string"
            },
            "format": {
              "description": "MIME-type of the source package resource.",
              "type": "string"
            },
            "upload_id": {
              "description": "Unique identifier for the source package upload resource.",
              "type": "integer"
            }
          }
        },
        {"type": "null"}
      ]
    }
  }
