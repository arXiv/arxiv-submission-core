"""This is here so setup.py will find it and consider it a package.

I suspect it might clash with the packages in other git repos. 

Early setup of arxiv-ng's git repos and python packages has left it all a mess."""
